---
monit_check_filesystems:
  - { name: rootfs, mountpoint: "/", usage_limit: 90% }

#monit_check_hostname: "{{ ansible_nodename }}"
monit_check_hostname:
monit_check_port: 80
monit_check_request: /

monit_mailfrom: root@example.com
monit_mailto: root@example.com
